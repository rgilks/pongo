{
  "name": "iso",
  "version": "0.1.0",
  "description": "Isometric arena shooter with Rust + WebGPU + Cloudflare Durable Objects",
  "scripts": {
    "check": "npm run check:rust && npm run check:web",
    "check:rust": "cargo fmt --check && cargo clippy --workspace -- -D warnings && cargo test --workspace",
    "check:web": "echo 'Web CI build + headless test - to be implemented in M3'",
    "fmt": "cargo fmt",
    "clippy": "cargo clippy --workspace -- -D warnings",
    "test": "cargo test --workspace",
    "test:game_core": "cargo test --package game_core",
    "test:all": "./scripts/test-all.sh",
    "dev": "wrangler dev",
    "deploy": "wrangler publish",
    "deploy:test": "./scripts/deploy-test.sh",
    "logs": "wrangler tail iso --format pretty",
    "logs:check": "./scripts/check-logs.sh 10"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20.0.0"
  }
}

